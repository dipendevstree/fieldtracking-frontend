/**
 * Toast Configuration
 *
 * Global defaults for toast notifications (Sonner).
 */

export const TOAST_CONFIG = {
  duration: 3000,
  position: "top-right" as const,
  showCloseButton: true,
  showIcons: false,
};
