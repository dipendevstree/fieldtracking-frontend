import AdminAuthForm from './Admin-auth-form'

const SignInSection = () => {
  // This component is used to render the admin authentication form
  // It is a simple wrapper around the AdminAuthForm component
  // It can be extended in the future to include additional features or styles

  return <AdminAuthForm />
}

export default SignInSection
